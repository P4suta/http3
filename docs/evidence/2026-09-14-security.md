# 2026-09-14 security

- Date: 2026-09-14
- Commit: 48a8b3b0a609b7c33ff54c571c9c5935ad41336f, with uncommitted
  changes to `.mise.toml` and `test/run_mix_gleam_package.sh`, both
  described in [the check evidence](2026-09-14-check.md). Neither touches `src/`.
- Gleam: 1.18.1
- Erlang/OTP: 29.0.5
- mise: 2026.8.6
- Host: Windows 11 Pro 10.0.26200 (build 26200) x86_64, AMD Ryzen 7 5800H,
  8 cores / 16 threads, 39.4 GiB

Command:

    mise run security

Result: **fail**, at `dependency-audit`. The failure is not Windows-specific:
`golang.org/x/crypto` v0.54.0, pinned indirectly by the quic-go interop peer,
now carries three advisories that were not published when this gate last ran
green. CI runs this same gate on `ubuntu-24.04`, so it is red there too.

    Total 1 package affected by 3 known vulnerabilities
    (0 Critical, 0 High, 0 Medium, 0 Low, 3 Unknown) from 1 ecosystem.
    3 vulnerabilities can be fixed.

    | OSV URL                      | PACKAGE             | VERSION | FIXED   | SOURCE                     |
    | https://osv.dev/GO-2026-6303 | golang.org/x/crypto | 0.54.0  | 0.55.0  | test/interop/quicgo/go.mod |
    | https://osv.dev/GO-2026-6354 | golang.org/x/crypto | 0.54.0  | 0.56.0  | test/interop/quicgo/go.mod |
    | https://osv.dev/GO-2026-6355 | golang.org/x/crypto | 0.54.0  | 0.56.0  | test/interop/quicgo/go.mod |

The pre-existing `.osv-scanner.toml` filter still applied cleanly and is
unaffected:

    GO-2026-5932 has been filtered out because: Interop-only quic-go reaches
    x/crypto/chacha20; the advisory applies to the unimported, deprecated
    openpgp package. Recheck when updating quic-go.

The advisory is in an interop test peer, not in `src/` or in either shipped
package, so no library code path is implicated. Closing it means raising the
pin to at least v0.56.0 with `go.sum` updated in lockstep and the interop
matrix rerun against Docker peers; that was deliberately left undone here
rather than hand-edited, because this host did not run the interop gate.

## Gate members that did pass

`dependency-audit` is the third of six members, so the run stopped before the
last three. Those were executed individually on the same host and commit:

| Member | Result |
| --- | --- |
| `secret-scan` (gitleaks) | pass |
| `semgrep` | pass, 5 rules on 57 files, 0 findings |
| `dependency-audit` (osv-scanner) | **fail**, three advisories above |
| `reuse lint` | pass |
| `ffi-audit` | pass |
| `sbom` (syft) | pass, `build/security/http3.cdx.json` written, 15592 bytes |

`semgrep` and `sbom` only reach this state because of the `shell = "bash -c"`
fix recorded in [the check evidence](2026-09-14-check.md); before it, both
died on `mkdir -p` under `cmd.exe`.
